<?php

/**
 * @return array
 */
function etype_newsletters_action_info() {
  return array(
    'etype_newsletters_add_node' => array(
        'type' => 'node',
        'label' => t('Add Node to Newsletter'),
        'configurable' => FALSE,
      ),
    );
  }

/**
 * @param $entity
 */
function my_application_reset_action($node) {
  var_dump($node);
  exit;
}